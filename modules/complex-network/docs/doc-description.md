# 复杂网络图

## 需求描述
用上中下三层节点表示暴露、代谢物与结局之间的关系，便于查看共享连接。

## 应用场景
- 可用于用上中下三层节点表示暴露、代谢物与结局之间的关系，便于查看共享连接，但需要先确认自己的数据结构和统计量定义与该编码一致。
- 当前展示的是授权示例或包内数据；套用于新的研究时须核对输入含义、缺失处理及上游统计。

## 数据特征
120 条关系、4 列；按实际唯一节点数定位，避免重复实体使索引错位。

## 视觉编码
上层暴露、中层代谢物、下层结局；点大小为连接频次，连线表示关系。
