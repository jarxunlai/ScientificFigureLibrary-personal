schema_version: open-figure-modules-data-schema/v1
description: Two example tables: DEG rows with fold-change/percentage fields and GO rows with direction and bar coordinates.
example_data: data/example_de.csv
constraints:
  - 示例数据只用于可移植绘图层验证，不代表原始研究数据。
  - 输入文件路径相对于模块根目录。
