# 热图局部放大

## 需求描述

在同一静态画布并排展示全矩阵与指定样本区域，便于核对局部模式。

## 应用场景

- 可用于在同一静态画布并排展示全矩阵与指定样本区域，便于核对局部模式，但需要先确认自己的数据结构和统计量定义与该编码一致。
- 当前图仅是模拟示例，用于检查布局；正式分析必须替换为经过审核的数据和结果。

## 数据特征

独立模拟的 26 个基因 × 172 个样本突变矩阵；放大前 20 个样本。
