# 分面双向柱加热图

## 需求描述
上下方向的条形图对照两类组学特征的重要性分布及类别。

## 应用场景
- 可用于上下方向的条形图对照两类组学特征的重要性分布及类别，但需要先确认自己的数据结构和统计量定义与该编码一致。
- 当前图仅是模拟示例，用于检查布局；正式分析必须替换为经过审核的数据和结果。

## 数据特征
脚本内固定种子模拟的特征值和类别；不需要原先未使用的大型输入表。

## 视觉编码
上下两面板，第二面板纵轴反向，共用类别颜色。
