schema_version: open-figure-modules-data-schema/v1
description: Synthetic marker-level rows with celltype, group, marker, impact score, EC score, and specificity.
example_data: data/fig5.csv
constraints:
  - 示例数据只用于可移植绘图层验证，不代表原始研究数据。
  - 输入文件路径相对于模块根目录。
