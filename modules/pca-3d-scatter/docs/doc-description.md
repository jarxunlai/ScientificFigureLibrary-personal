# 三维嵌入坐标散点图

## 需求描述
用三维坐标展示分组和分支结构，不将预先给出的坐标误称为 PCA 结果。

## 应用场景
- 可用于用三维坐标展示分组和分支结构，不将预先给出的坐标误称为 PCA 结果，但需要先确认自己的数据结构和统计量定义与该编码一致。
- 当前图仅是模拟示例，用于检查布局；正式分析必须替换为经过审核的数据和结果。

## 数据特征
300 个独立模拟三维坐标、三个分组；未执行 PCA 或扩散映射算法。

## 视觉编码
固定视角三维散点，坐标标为 DC_1、DC_3、DC_2，颜色区分模拟分组。
