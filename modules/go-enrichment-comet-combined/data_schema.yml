schema_version: open-figure-modules-data-schema/v1
description: A fully synthetic long table with Cluster, Description, Count, pvalue, and -log10(p) fields.
example_data: data/enrichment_comet.csv
constraints:
  - 示例数据只用于可移植绘图层验证，不代表原始研究数据。
  - 输入文件路径相对于模块根目录。
