# 形状火山图

## 需求描述
用点形状区分附加分组，同时显示效应方向和显著性。

## 应用场景
- 可用于用点形状区分附加分组，同时显示效应方向和显著性，但需要先确认自己的数据结构和统计量定义与该编码一致。
- 当前图仅是模拟示例，用于检查布局；正式分析必须替换为经过审核的数据和结果。

## 数据特征
脚本内模拟 1000 个基因的 log2FC、FDR 及随机组别，不依赖隐藏的示例对象。

## 视觉编码
横轴 log2FC、纵轴 -log10(FDR)，红蓝方向色、两种形状和阈值虚线。
