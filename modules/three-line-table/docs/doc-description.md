# 三线表

## 需求描述
用分组汇总表并列展示不同结局的数量、百分比与连续指标中位数。

## 应用场景
- 可用于用分组汇总表并列展示不同结局的数量、百分比与连续指标中位数，但需要先确认自己的数据结构和统计量定义与该编码一致。
- 当前图仅是模拟示例，用于检查布局；正式分析必须替换为经过审核的数据和结果。

## 数据特征
720 条独立模拟观测，六个虚拟医院；分组与总计使用同一纳入范围并检查数量一致。

## 视觉编码
双层分组表头、三条主要水平线、总计行加粗；无需浏览器渲染。
