# 多组哑铃图

## 需求描述
比较同一特征在多个组中的效应位置，并用点大小补充显著性信息。

## 应用场景
- 可用于比较同一特征在多个组中的效应位置，并用点大小补充显著性信息，但需要先确认自己的数据结构和统计量定义与该编码一致。
- 当前图仅是模拟示例，用于检查布局；正式分析必须替换为经过审核的数据和结果。

## 数据特征
脚本内模拟 20 个特征 × 3 组，含 log2FC 与 P 值。

## 视觉编码
每行多组端点及范围线，点大小明确为 -log10(P)，不是 FDR。
