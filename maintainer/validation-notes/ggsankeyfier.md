# ggsankeyfier validation note

- Scope: synthetic data only.
- Publisher execution: the cleaned `code/organized.R` was run in the isolated project Pixi environment with R 4.5.3 and the required plotting packages.
- Output: three PNG validation figures and one generated main preview were written to an ignored staging directory.
- SFL execution: false; SFL only validates, downloads or reads, and extracts archives.
- Excluded: source screenshots, book pages, PDFs, original unredistributed code, real data, and local environment state.
